export const imageWidth = 330;
export const imageHeight = 330;

// File size limits

const MB_SIZE = 1024 * 1024;

export const AUDIO_FILE_SIZE_LIMIT_MB = 4; // 4 MB
export const AUDIO_FILE_SIZE_LIMIT = AUDIO_FILE_SIZE_LIMIT_MB * MB_SIZE;

export const IMAGE_FILE_SIZE_LIMIT_MB = 4; // 4 MB
export const IMAGE_FILE_SIZE_LIMIT = IMAGE_FILE_SIZE_LIMIT_MB * MB_SIZE;
